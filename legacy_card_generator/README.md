# Legacy card generator (Node.js)

Historical reference for the original interactive CLI + canvas renderer.

**Use the TypeScript API at the repo root instead** (`npm run dev`).

See the root [README](../README.md).
